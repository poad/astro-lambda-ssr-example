{
  "private": true,
  "scripts": {
    "lint": "pnpm -r --if-present --parallel run lint",
    "lint-fix": "pnpm -r --if-present --parallel run lint-fix"
  },
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "^11.20.0",
      "onFail": "download"
    },
    "runtime": {
      "name": "node",
      "version": "^24.18.1",
      "onFail": "download"
    }
  },
  "devDependencies": {}
}
