{
  "private": true,
  "scripts": {
    "lint": "pnpm -r --if-present --parallel run lint",
    "lint-fix": "pnpm -r --if-present --parallel run lint-fix"
  },
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "^11.15.1",
      "onFail": "download"
    },
    "runtime": {
      "name": "node",
      "version": "^24.18.0",
      "onFail": "download"
    }
  },
  "devDependencies": {}
}
